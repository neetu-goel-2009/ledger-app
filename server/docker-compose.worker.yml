version: '3.8'
services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
    restart: unless-stopped

  worker:
    build:
      context: .
    command: python server/worker.py
    depends_on:
      - redis
    env_file:
      - .env
    restart: unless-stopped
